import { TabsContent } from "@/components/ui/tabs";
import React from "react";

const PersonalSettings = () => {
	return (
		<TabsContent
			value="Personal"
			className="rounded-xl flex flex-col gap-y-6 "
		>
			<p>Personal settings (Work in progress)</p>
		</TabsContent>
	);
};

export default PersonalSettings;
